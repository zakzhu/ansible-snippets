- name: <> extract SRC into DEST
  unarchive:
    src:
    # Remote absolute path where the archive should be unpacked.
    dest:
    #owner:
    #group:
    #mode:
    # If the specified absolute path (file or directory) already
    # exists, this step will *not* be run.
    creates:
    #list_files: yes
