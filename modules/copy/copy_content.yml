- name:
  copy:
    content: |

    dest:
    #owner:
    #group:
    mode: "0644"
    # The validation command to run before copying into place.
    # E.g. `validate: /usr/sbin/visudo -csf %s`
    #validate:
